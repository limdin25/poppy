// Instagram is temporarily hidden: HeyPubli currently runs as a pure affiliate
// platform (email signup + tracking links only). Set NEXT_PUBLIC_INSTAGRAM_ENABLED=true
// in Vercel and redeploy to bring every Instagram flow back — nothing was removed.
export const INSTAGRAM_ENABLED = process.env.NEXT_PUBLIC_INSTAGRAM_ENABLED === "true";
