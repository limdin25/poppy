# Decisions Log

Every architectural choice and why.

---

| # | Decision | Rationale | Date |
|---|----------|-----------|------|
| 1 | New repo instead of extending Lemlin | Keeps projects isolated, avoids accidental breakage. Different target market (UK SMBs vs IG automation) means different growth paths. | 2026-05-01 |
| 2 | Copy UI primitives from Lemlin | 14 proven Radix/shadcn components already built and tested. Saves 1-2 weeks of boilerplate. | 2026-05-01 |
| 3 | Roger Roger color scheme (blue `#428EF4` CTAs, clean white) | Modern, minimal aesthetic. Blue conveys trust and professionalism for business users. | 2026-05-01 |
| 4 | Inter font | Clean, professional, widely used in SaaS. Excellent readability at all sizes. | 2026-05-01 |
| 5 | Retell AI + Twilio for voice (not building voice AI from scratch) | Months of audio engineering saved. Retell handles STT/TTS/turn-taking, Twilio handles telephony. We focus on the AI brain and UX. | 2026-05-01 |
| 6 | Claude Sonnet 4.6 as AI brain (not GPT) | Strict tool use mode guarantees valid parameters. Critical for booking appointments and generating quotes mid-call without hallucinated fields. | 2026-05-01 |
| 7 | Mobile-first design | Users (business owners) primarily check their receptionist from their phone. Desktop is secondary. | 2026-05-01 |
| 8 | Feature isolation pattern | Same modular architecture as Lemlin. Features cannot import other features (ESLint enforced). Prevents cascading breakage when one feature changes. | 2026-05-01 |
| 9 | Reuse marketplace10's Unipile credentials and pattern | Unipile account already connected and paid for hub.nfstay.com. Same token/DSN works for Poppy — no extra cost. Webhook + polling dual approach copied from marketplace10 because Unipile webhooks are unreliable (proven in production). | 2026-05-02 |
| 10 | String equality webhook auth (not HMAC) | Unipile only supports `Unipile-Auth` header with a shared secret string. No HMAC signing available. Acceptable for now; the secret is stored in Vercel env vars, not in code. | 2026-05-02 |
| 11 | ~~gpt-4o-mini~~ → Claude Sonnet 4.6 for WhatsApp auto-replies | Switched to Claude for consistency across the stack. One AI provider, one prompting style, one billing line. Claude Sonnet is fast enough for chat replies and better at following the business system prompt. | 2026-05-03 |
| 12 | Download attachments to Supabase Storage (not hotlink Unipile URLs) | Unipile attachment URLs are temporary/authenticated. Storing in our own Storage bucket gives permanent public URLs and no vendor lock-in for media display. | 2026-05-03 |
| 13 | Regex filter for reaction notification texts (not just `is_event` flag) | Unipile sends reaction notifications with `is_event=0` sometimes. Belt-and-braces: filter by both the `is_event` flag AND by detecting the text pattern `"{{phone@s.whatsapp.net}} reacted X"`. | 2026-05-03 |
| 14 | Vercel CLI deploy with `.git` hide trick | Vercel project is linked to `hrds100/poppy` repo but code lives in `limdin25/poppy`. Auto-deploy doesn't trigger. Workaround: `mv .git .git.bak && vercel deploy --prod && mv .git.bak .git`. Works reliably. | 2026-05-03 |
| 15 | Video as first-class content type | Added `'video'` to the messages content_type constraint alongside text/audio/image/file. Frontend renders `<video controls>` with the media_url. Future-proofs for WhatsApp video messages. | 2026-05-03 |
| 16 | AI draft replies in poll.ts (not just webhook) | Unipile webhooks are unreliable — many WhatsApp messages arrive only via polling. Poll.ts now generates AI draft replies for new inbound messages, same as the webhook path. Belt-and-braces. | 2026-05-03 |
| 17 | Draft rewrite + inline edit (not just approve/discard) | Users need control over AI drafts before sending. Added rewrite (regenerate via Claude) and edit (inline textarea) buttons alongside discard/approve. Works for all channels. | 2026-05-03 |
| 18 | Reuse existing Retell agent + SIP trunk (not rebuild from scratch) | Hugo already had Retell agent, LLM, SIP trunk, and imported phone number configured. Updated the LLM prompt to Elsie's format and pointed webhook to our endpoint instead of n8n. | 2026-05-03 |
| 19 | Two-step Retell setup: LLM then Agent | Retell API requires creating an LLM object first (holds the system prompt), then an Agent referencing the llm_id. The agent itself has no prompt field. | 2026-05-03 |
| 20 | Claude Sonnet for call transcript extraction (not OpenAI) | Consistent with our AI stack — one provider, one billing line. Claude extracts caller name, phone, reason, summary, action_required from transcripts. | 2026-05-03 |
| 21 | Web Crypto API for webhook verification (not Node.js crypto) | Vercel edge runtime doesn't support Node.js `require('crypto')`. Retell signature format is `v=timestamp,d=hmac_hex` using HMAC-SHA256 with the API key as secret. | 2026-05-03 |
| 22 | No long dashes, anywhere, ever | Hugo's standing rule, 2026-07-27. Applies to code, comments, docs, UI copy, customer messages, AI prompts and replies to him. It is not only taste: a text is GSM-7 (160 characters a segment) only if every character is in the GSM 03.38 table, and one em dash flips the whole message to UCS-2 at 70 characters a segment. The old video text was 164 characters and cost 3 texts; the rewritten one is 211 characters, longer and warmer, and costs 2. Enforced by `tests/message-copy.test.ts`, which fails the build. Helpers in `api/lib/sms-charset.ts`. Curly quotes and the ellipsis character do the same damage and are banned with it. | 2026-07-27 |
| 23 | Video send text invites a reply, and says when there is no website | Hugo, 2026-07-27: "if you have any question please just shoot me a message at any time" and "you say you don't have a website when they don't have". Both variants now close with "Any questions, just message me here any time", laid out in paragraphs with the link on its own line so it is tappable on a phone. Both variants send the SAME message, because the audit runs from Google either way; the earlier no-website wording claimed it ran "from your Google listing instead", which read as a difference where there is none. The no-website variant only ADDS a P.S. | 2026-07-27 |
| 24 | Free-website offer REINSTATED, no-website variant only | Hugo, 2026-07-27, reversing his own withdrawal of 2026-07-26: "I couldn't find website. If you don't have one, you can make one for free, just let us know." It rides only `send_template_no_site`, as a P.S. after the identical message everyone else gets, so a lead who already has a site is never told we could not find one nor offered a second. Costs a third SMS segment (339 characters vs 211); Hugo asked for it knowing the message gets longer. `tests/message-copy.test.ts` and `tests/vsl-funnel.test.ts` previously asserted the offer stayed withdrawn and now assert the opposite, with the full history in the comments so nobody "fixes" it back. | 2026-07-27 |
| 27 | Builder roster is its own admin-only table, matched to a house by plain postcode outcode (not geodistance) | Hugo, 2026-08-14: sourcing houses nationwide means one builder near Pedro can't be the viewer for every house, so a roster of local builders is matched by postcode when a viewing is booked. `brrr_builders` follows the exact `brrr_properties` pattern: no RLS, service-role only via `api/admin/builders`. Matching reuses the existing `outcodeOf()` from `api/lib/brrr-deal-facts.ts` rather than a new geocoding dependency: exact outcode match first (e.g. `LE7`), falling back to the bare area prefix (`LE`) only when nobody covers the exact outcode. No match returns nothing, never a guessed "nearest" builder. See [BRRR_STRATEGY.md §9a](../../scraper/BRRR_STRATEGY.md). | 2026-08-14 |
| 28 | `npx tsc --noEmit` at the repo root silently checks nothing | Discovered 2026-08-14 shipping the builder roster: `tsc -b` (what `npm run build` actually runs) caught a real type error — `useAdminMutation`'s method union was missing `'DELETE'` — that `npx tsc --noEmit` had reported zero errors on moments earlier. The root `tsconfig.json` is solution-style (`"files": []`, only `references`), which plain `tsc --noEmit` does not build, so it type-checks nothing and exits clean regardless of real errors. The documented test-loop command (`npx tsc --noEmit && npx vitest run`) needs `tsc -b` in place of `tsc --noEmit` to mean anything; until that's changed, verify with `npx tsc -b`. | 2026-08-14 |
| 25 | The offer strip above Pedro's script FOLDS, and starts folded | Hugo, 2026-08-14: "please give option to colapse all this and start colapsed so pedro can read the script property." The strip had grown to six rows (next step, address, strategy and band, three money cells, evidence, reason) and was pushing the script off the fold, which is the opposite of why it was pinned there. Collapsed it is one line: address, asking price, which of the two calls this is. No figure of ours is on screen until he opens it. `startCollapsed` is passed by the dialer ONLY; Call history leaves it open, because there the deal is the thing being read. | 2026-08-14 |
| 26 | Every property call writes a next-step brief onto the house | Hugo: "build the brain that always after the call gets the exactly instruction for the next step for me to read." A stage tag says where a deal is, not who does what with which number. `api/lib/next-step-brief.ts` writes verdict, asking and opener, why it holds, the instruction, the blockers, the ladder, the board column and a confidence level into `brrr_properties.brief` on EVERY outcome, and the same text is the notification body. Deterministic, no model, same law as `nextStep.ts`. Every figure is read from `offerRange`/`ladderText`/`brrr-deal-facts`, never re-derived. A missing fact becomes a blocker, never an assumption. | 2026-08-14 |
| 27 | Hugo's pinned note is a column of its own, never `notes` | `brrr_properties.notes` holds the scraper's listing description, and nothing on screen ever read it. `pinned_note` is human-written only, survives every re-scrape (the ingest upsert does not carry it), and renders ABOVE the machine's brief because a human decision outranks a generated one. Written from the admin Properties drawer (`save_note`), never by the outcome route. | 2026-08-14 |
| 28 | The 16-question checklist MERGES instead of replacing | The checklist opens blank on every call and the outcome route wrote it straight through, so ringing a branch twice wiped everything the first call learned, including the figure. A blank field now means "not asked this time", not "the answer is nothing". Found while building the brief, whose confidence score is computed from exactly those fields. | 2026-08-14 |
| 29 | The email to the branch is asked for, written and SENT on the call | Hugo: "he asks can I have your email, they give it, and he says I'm sending you an email now, please confirm you received it." Its own tab in the calling room, property calls only. The address types itself from what the coach hears (`api/lib/spoken-email.ts` handles "d dot smith at zest dot co dot uk"); the copy is written from the live transcript; the fixed template is always in the box as the fallback. An email the agent watched arrive gets read, and a mistyped address is caught while they are still on the line instead of when an offer silently never lands. | 2026-08-14 |
| 30 | A refused video must never cost us the email address | The email ask used to hang off the video ask, so a branch that said no to one said no to both. The video is a bonus, the address is the call: it is now its own question with the word "anyway" in it, and there is a second two-line email ("No video, just my address") that asks for NOTHING, because a branch that has just refused one thing will refuse a second ask in the same breath. | 2026-08-14 |
| 31 | Call one's email can never carry a figure, and there are three fences | The prompt forbids it, the writer is never given the numbers at all (`videoFacts` carries no offer, asking, GDV or refurb), and a draft that names one anyway is refused with a 422 so the plain template sends instead. `address_only` sits inside the same guard as `video_request`. A rule a model is told can be broken; a number it was never given cannot be. An email cannot be unsent. | 2026-08-14 |
| 32 | `wk-email-send` accepts a typed recipient (`to_email`) | Sister branches of one agency routinely share a single inbox and `wk_contacts` has a UNIQUE index on email, so storing the address first is not always possible. Refusing to send because another branch already holds that address would be absurd. The address is validated server-side, and written onto the lead AFTER the send, best effort. | 2026-08-14 |
| 33 | `firstText()` is the only way to read an Anthropic response | THE BUG: `claude-sonnet-5` answers with a `thinking` block FIRST, so `content[0].text` is undefined and `callLLM` returned an empty string every single time. Both AI email drafts had failed on production from the day they shipped ("The model did not answer") while the API call itself was perfectly fine. Found by replaying the request by hand against the production key and reading the raw JSON. `api/lib/anthropic-content.ts` is a pure module (so it is testable without a Supabase client) and the other five Anthropic readers in `api/` now use it too: they resolve to sonnet-4-6 and worked, and would have gone silent the day anyone switched `platform_settings.ai_model`. | 2026-08-14 |
| 34 | The transcript table columns are `body` and `ts` | `draft-offer-email` asked for `text, created_at`, which do not exist on `wk_live_transcripts`. PostgREST 400d, the catch swallowed it, and every AI offer email was written as though the call had never happened. A failed read is now logged instead of silently becoming "no transcript". | 2026-08-14 |
| 35 | An unknown coach card kind must never white-screen the coach | `COACH_ICONS[event.kind]` was undefined for any kind this build had not heard of, and the render reads `.colour` off it. The server and the browser deploy separately, so a new kind can arrive at any moment, mid-call. It now falls back to the suggestion styling. | 2026-08-14 |
| 36 | Water is its own question on every property call | Hugo: "very important on the prompts to find out if there is any leaking, any roof problems." It was one word inside "the big four", which is not the same as asking. Now asked separately on every house with two follow-ups that get past "I think it's fine", plus the rule that governs the reaction: a leak is not a reason to walk away, it is the reason the price comes down, so never react and never say what it costs us. Pinned by `tests/property-script-isolation.test.ts` in both the script and the coach's copy. | 2026-08-14 |
| 37 | The EPC ask stays, because the register only answers for one house in ten | Measured on the live VPS on 2026-08-14: `rm_epc_subject` holds 4,160 lookups and 394 matches, 9.5%. The register keys on a house number and listings rarely print one, and it returns the floor area only, not the boiler, insulation or walls. So asking the branch is the fallback for the other 90%, not a duplicate. | 2026-08-14 |
| 38 | Comparables are priced per SQUARE METRE, and a rate far off the median is thrown out | `build_course_list.py` priced every house with `gdv = prices[len(prices) // 2]`: size-blind, no outlier rejection, and on an even count that is the UPPER middle, not the median. 39 Orion Way (2 bed, 55.6 sqm) had comps [105000, 110000, 139000, 210000] and index 2 is 139,000, which is the GDV that reached Pedro. One comp sold at GBP 3,500/sqm on a GBP 1,650 street. The two houses on Orion Way itself say GBP 92,667. We opened at GBP 96,375, ABOVE what the house is worth done up, on a business whose whole model is buying below value. `comp_value.py` prices per sqm, drops rates outside 0.6x to 1.6x of the median and records them as evidence, and falls back to a true raw median only when sizes are missing, saying so in `gdv_basis`. | 2026-08-14 |
| 39 | The per-sqm sanity band is built round the median, never the extremes | `valuation.py`'s cross-check used the cheapest and dearest rate in the set as its band, so one bad comp widened it until the clamp could never bite, which is the opposite of what a sanity check is for. On Orion Way it put the ceiling past GBP 194,000 on a 55.6 sqm house. Now `comp_value.ppsm_band()`. | 2026-08-14 |
| 40 | The evidence standard is enforced at the last gate before Pedro, not only on the engine | Only gold and strong ship, and the engine has enforced it since 2026-08-13. But 110 of the 180 houses on the board were fair, good or last_resort: they arrived on the RETIRED hand push, which bypasses the gate, and nothing on the CRM side re-checked them. `scripts/lib/evidence-standard.mjs` repeats the rule where the queue is built and the run prints what it held back. A property with NO tier recorded is refused too, because an unrecorded tier means the deal predates the standard, which is exactly the population that needed catching. Enforcing it took the board from 128 branches to 55. | 2026-08-14 |
| 41 | The board was re-valued in place, and nothing was withdrawn | 157 live properties re-valued after the maths fix. 76 GDVs fell, 31 rose, 59 unchanged; 33 had ALREADY been offered above the corrected ceiling (worst: Orion Way, over by GBP 28,030). The old figures are kept verbatim under `deal.superseded` and the affected rows carry `deal.offered_above_ceiling`, because a number already said out loud on a phone call cannot be unsaid and deciding what to do about it is Hugo's, not the script's. Backups: `brrr_deal_backup_20260814`, `wk_contacts_cf_backup_20260814`, `wk_contacts_cf_backup2_20260814`. | 2026-08-14 |
| 42 | Correcting a property does nothing until the CONTACT is corrected | The live coach reads `offer_open`/`offer_ceiling`/`offer_ladder` off `wk_contacts.custom_fields`, not off the property, so the re-valuation changed nothing Pedro would say. Orion Way's house said GBP 64,074 while his card still said GBP 96,375. `assign-properties-to-pedro-houses.mjs --refresh --apply` is the designed path (its own comment: "a valuation that arrives, or a maths fix, after the branch was queued"). Then 152 contacts whose branch has no live house, and 63 more whose branch has no gold/strong house, had their money keys stripped. Result: every figure Pedro can say out loud rests on a live, shippable deal. | 2026-08-14 |
| 43 | The ballpark comes from the CALL, not from the overnight run | Hugo: "after the first call when Pedro clicks discovery, that's when our AI does a stress test against that property based on the information the agent has said." The overnight number is a SCREENING number, built from photographs with nobody having spoken to anybody, and Pedro never says it out loud. `reprice.py` rebuilds GDV, refurb, TMV and the band from what the agent actually said. Refurb is taken at the LOW end on Hugo's instruction, because the budget is what we hand the builder, not a prediction. The money is computed on the ENGINE and nowhere else: the CRM extracts the facts (language work) and never does the arithmetic. | 2026-08-14 |
| 44 | An unknown condition is a refusal, and a derelict asks for a builder | An unknown condition priced at zero is a turnkey, which is the most expensive possible wrong answer since a lower refurb RAISES the offer. And `refurb_model.UNPRICEABLE_BANDS` refuses derelict on purpose: the first version of the re-valuation fell back to a provisional modernisation figure for those 8 houses, which would have put a GBP 7,000 refurb on a shell needing GBP 60,000, the exact direction that produces an offer we cannot honour. | 2026-08-14 |
| 45 | `floor_area.py` runs every night | It was never in `overnight_pipeline.sh` and had last been run by hand on 2026-08-12, so two days of floor plans never reached the gate. It matters more since comps are priced per sqm (#38): a house with no size falls back to a size-blind median, and the area is unknown on about 70% of judged properties. The rebuild took the cache from 1,485 to 1,893. It runs AFTER `epc_subject_fill` (which it calibrates against, a sum of rooms runs about 1.27x small) and BEFORE `fast_comps` (which picks comps on size). 3,779 floor plans we already hold are still unread and about 36% of a read yields an area, which is the biggest remaining lever on accuracy. | 2026-08-14 |
| 46 | The inbound email is READ, not just filed | Hugo: "Lexi has replied but the system is still suggesting a follow up ... the system has not synchronized." The webhook filed her email and did nothing: no classification, no figure, no notification, no change to the card, so the board said "Chase the agent" for seven hours after the answer arrived. `api/lib/inbound-classify.ts` reads it on plain keywords first (deterministic, free, cannot be rate-limited or hallucinate), a model pass may refine it and is validated against the same closed list, and the model's figures are thrown away and re-read from the text. Three subtleties: an out of office is checked FIRST (an autoreply quotes our own offer email underneath and would match every rule below it), a rejection that names a figure is a COUNTER-OFFER (a door opening at a price, not a door closing), and supplying the video we asked for changes the deal exactly as much as refusing to. | 2026-08-14 |
| 47 | `stepForInbound` rewrites the instruction ONLY when the new one is obvious | A rejection or counter becomes `Renegotiate`, an acceptance becomes `Get it in writing`, a closed door clears it, and everything else returns null so the existing tag stands and a human decides. A wrong instruction is worse than a stale one, because Pedro reads these out loud. A test pins every tag it can write against `dealProcessSteps.ts`. | 2026-08-14 |
| 48 | A virus disclaimer is not a rejection | Found by replaying all 161 real inbound emails rather than by reading the code. Four of them, DDM Residential's own included, carry "does not accept liability" in the virus footer, and a rejection rule matching a bare "not accept" fired on 32 of the corpus. `stripDisclaimer()` cuts the boilerplate once up front (it is noise on EVERY rule, not just that one) and "not accept" must now attach to the offer. Result: 3 rejections across 161 emails, two real and one counter-offer, which is Lexi on 39 Orion Way. | 2026-08-14 |
| 49 | Money in an email is currency-marked or comma-grouped, never a bare run of digits | Lexi's signature carries "01472 358671" and a mobile, and a bare-digits rule read them as GBP 358,671 and GBP 3,844,565. A phone number quoted back as a price in a notification is worse than missing a figure, and the KIND of a reply is judged on words rather than numbers so nothing important rests on it. The alternation order was wrong too: it matched "963" out of "GBP 96370" then discarded it as under a thousand. `tests/fixtures/inbound-emails.json` keeps all 161 real emails so this is regression-tested without a database. | 2026-08-14 |
| 50 | The Deal Manager: the AI decides attention and words, code decides money and moves | Every brain in the machine fired on a human action and then forgot, so nothing watched a deal BETWEEN events. `api/lib/deal-state.ts` assembles everything known about one deal deterministically and is the ONLY thing the Manager is ever shown, which is what makes the fences checkable: a figure not in that object was provably invented. `deal-manager-contract.ts` holds the fences: every number in an instruction must already be on the file (no arithmetic, no splitting a difference), the action comes from a closed list PER STAGE so a step cannot be invented, closed flags, a bounded score, and the long-dash rule. Every failure has the same answer: fall back to the deterministic brief, log the refusal, carry on. `platform_settings.deal_manager.enabled` defaults OFF and unreadable settings mean OFF, so turning it off returns the product to exactly what it is today. | 2026-08-14 |
| 51 | The counter-offer reply decides raise, hold or pass in CODE | Hugo: "AI should draft the email reply ... and then decide if we can increase the offer or just pass." `api/lib/counter-position.ts` reaches the decision and the exact figure; the new `counter_reply` prompt is handed the answer and told to write it, and the only figures it is given are the ones that decision allows. A model asked "should we go up?" finds a reason to say yes, because that is where the conversation pulls. Two refusals rather than a guess: no ceiling on file means hold, and evidence below gold/strong means hold. The invariant (no decision ever proposes paying above the ceiling) is swept across ~600 input combinations. `counter_reply` is the ONLY kind allowed to talk price after an offer has gone out, which is why `follow_up`, forbidden from re-opening price, could not answer Lexi. | 2026-08-14 |
| 52 | A branch that NAMES one of our houses is matched to it | The domain rule cannot help when an agency's domain is nothing like its trading name (Zest of Hull is movewithzest.co.uk), and it correctly refuses rather than attaching a stranger. That refusal stays exactly as strict. `matchByNamedHouse()` runs AFTER it and uses far stronger evidence: the branch naming the street in their own words. THE FLOOR IS DISTINCTIVENESS, NOT LENGTH: "The Green" is nine characters with a space exactly like "Orion Way", so a street counts as evidence only when it carries a word that is not generic. Refuses on two matches, like every other rule here. | 2026-08-14 |
| 53 | The engine answers the ballpark over HTTP, and the CRM never does the arithmetic | `POST /api/reprice` on the scraper app, same shared secret as the ingest direction, opened in nginx so Vercel can reach it. Proven live on a gold-tier deal: cosmetic gives an opener of GBP 115,306 and full_refurb GBP 104,933, a ten thousand pound gap that is exactly what the four condition questions on call one exist to close and exactly what a flat GBP 10,000 could never see. 401 without the secret, 422 below the comps standard, and a derelict asks for a builder. | 2026-08-14 |
| 54 | The Today list is ordered by code, not by the model | Gap 5 of the Deal Manager plan: the queue is ordered once a night, then overdue follow-ups, fresh replies and booked call-twos compete by eye. `TodayPanel` sorts on `baselineAttention`, so the order is right with the deal brain switched off, and a branch that wrote to us and was ignored outranks everything because that is the one that was actually costing money. It shows what the branch said next to the instruction that predates it. | 2026-08-14 |
| 55 | The advert's own stated floor area is read, and it outranks every measured source | 104 listings state a size ONLY in their free text ("approx. 850 sq ft of accommodation") and it was being thrown away while we paid a model to squint at the floor plan. `text_area.py` on the engine parses it with the garden trap closed (a 200 sqm garden must not become the house, context words refuse the match), takes the largest survivor so "lounge 18 sq m ... in all 84 sq m" reads 84, and stores the exact snippet so every number is auditable back to the agent's words. New source `listing_text` in `resolve_floor_area`, after `rightmove` and before every measured source, because both are the agent's own claim about the whole house. Chain order pinned by `test_area_sources.py`. | 2026-08-15 |
| 56 | Two floor-area levers measured and REFUSED, so nobody re-litigates them | Widening the EPC match: the register keys on a house number, ~6% of listings print one, and any looser rule prices house number 4 off house number 40's square metres, the exact bug the strict matcher was built to kill. A 3-sized-room calibration band in `floor_area.py`: only 13 EPC-paired 3-room plans exist and their EPC/sum ratio runs 1.10 to 2.17, so the "calibration" would be a coin toss with money on it. Both refusals are recorded in `text_area.py`'s header, next to the thing that DID work. | 2026-08-15 |
| 57 | The vision bill moves to the cheaper vendor, measured before trusted | Hugo: "stop using gemini api and use the cheaper alternative please, unless its not possible." Both volume readers (`floorplan_reader.py`, 11,612 plans scored; `property_brain.py`, the eye) gain an OpenRouter backend on the same qwen model and harness `second_room.py` has billed at GBP 0.000276 a read, ~70% under Gemini's measured USD 0.0009. The eye's parts list is TRANSLATED not rebuilt, so the two posters cannot drift on what the model is shown. "auto" prefers OpenRouter only when its key exists and degrades to Gemini otherwise. The cost challenger STAYS on Gemini deliberately: cross-vendor disagreement is what makes `cost_disputed` an honest check. `agree_backends.py` shows both models the same already-answered plans and condition reads and counts disagreements on the fields that move money; the verdict decides the default. | 2026-08-15 |
| 58 | 100 unique branches a day: not under the locked gate, and the numbers say why | Measured 2026-08-15: 31 houses pass the full gate, 29 unique branches TOTAL standing, ~20 queueable after the 14-day called-hold, a handful of new passes nightly. The wider universe holds 2,265 unique branches with at least one callable house (price band, phone, no auction/tenants/retirement), about three weeks of first-touch runway at 100/day. The one path to 100/day is discovery-first calling (call one never names a figure BY DESIGN, and the ballpark is rebuilt after the call via /api/reprice), with priced gold/strong deals as the priority tier on top. That bends the three musts, which are Hugo's locked rule from 2026-08-13, so it is presented as his decision and not taken. | 2026-08-15 |
| 59 | The vision flip STANDS, on measured agreement, not hope | Head-to-head on properties Gemini had already answered. Floor plans: 25/25 readable by qwen, 72-80% field agreement (those fields feed the retired swipe screen, not money). Condition bands, the field that sets the refurb: 15 compared, 10 identical, 5 one band off, ZERO two-plus bands off, and the drift direction splits 3 heavier (safer) to 2 lighter, so it is noise, not bias. Standing guards: the cross-vendor cost challenger kills >25% cost disagreement, confidence gating routes doubtful reads out, and the nightly canary bounds the batch. One bug found on the way: the eye's posters return (text, usage) and the OpenRouter one returned a bare string, so Python unpacked it character by character and every read "failed", which looked exactly like a model that cannot see and was actually a tuple. Both readers bill OpenRouter from tonight, ~70% off; Gemini remains as the automatic fallback and the challenger's vendor. | 2026-08-15 |
| 60 | NEVER edit overnight_pipeline.sh while the service is active | Bash reads a running script INCREMENTALLY from its byte offset. The text_area.py step was scp-ed over the file mid-flight on 2026-08-15; the running interpreter resumed at a shifted offset, died on "syntax error near unexpected token done" at line 217, and the night ended after the send step but BEFORE the assign: 40 houses reached the CRM and ZERO branches reached Pedro. bash -n had passed, because the file on disk was fine, it was the running process's read position that was corrupted. Recovery: assign run by hand at 11:15 (15 branches queued, 87 properties to human, figures refreshed on 50 held-back branches), morning report sent, failed unit reset, tonight's timer intact. Rule: deploy pipeline edits only while the service is inactive, or scp to a temp path and mv over it, because rename keeps the old inode alive for the running reader and an in-place overwrite does not. | 2026-08-15 |
| 61 | Discovery-first calling, 150 a night, authorised by Hugo in his own words | "we aim for 150 per night ... make sure pedro already have 150 now and then keep the circle by adding 150 new every night ... gold/strong on the top of pedros list ... i prefer 1 branch, 1 property." The three musts still gate every PRICED deal; discovery calls (which never name a figure, by design) go underneath to up-to-150 branches a night from a measured pool of 1,887. One branch, one property (its most motivated house: price cut first, then longest listed). Still verified: phone, house-not-flat, band 40-200k, no auction/tenants/retirement/shared-ownership, lettings sanity, crime gate, 14-day hold, DNC, dedupe, and NO money keys on the card so the coach cannot quote a figure that does not exist. `discovery_pool.py` + `scripts/assign-discovery-branches.mjs`; discovery inserts BELOW the minimum pending priority so it can never outrank a priced deal; SHORTFALL printed in capitals the first night 150 cannot be found. First fill ran 2026-08-15: 150 queued live, 25 held as already-called, 28 already queued, zero money keys on any discovery card. | 2026-08-15 |
| 62 | No comps within 400m, no discovery call | Hugo: "no point to call agents without comps, at least 400m ... as per our course, if no comp we cant sell it." The discovery pool now requires at least 3 sold comparables within 400 metres sold inside 24 months (the course's loosest tier at its quarter-mile radius), and branches whose comps are all inside 12 months sort FIRST, so Pedro's daily batch leads with houses that can be priced gold/strong the moment the call ends. Measured: pool 1,887 to 1,489, of which 1,055 are priceable-now. Today's queue was pruned against the rule (13 rows out) and topped back to exactly 150. `--count` became a TARGET, not an increment, because "add 150 every night" must never mean a growing stale backlog on a day Pedro dials fewer; the first fill double-counted to 287 before that change and was trimmed. | 2026-08-15 |
| 63 | The to-call list was wiped and rebuilt fresh; the worked branches never move | Hugo: "the ones we are nurturing always stay there, but for the list to be called, clear everything again and now give me the first 150 branches with all the requirements." PENDING queue rows only: pipeline cards, nurturing, follow-ups, call history and in-flight dials untouched (backup first: `wk_queue_backup_20260815`, 269 rows). Rebuilt in order: priced assign first (20 gold/strong branches, on top), then discovery top-up to exactly 150 underneath, every one carrying 3+ sold comps within 400m/24mo, one branch one property, no money keys. Final list verified by SQL: 170 pending, top 8 all PRICED, zero discovery cards with money. The wipe was a ONE-OFF reset; the nightly keeps top-up semantics (priced assign, then discovery to 150), which refills what Pedro dialled and never builds a stale backlog. | 2026-08-15 |
| 64 | The ballpark button: call one to call two is now one press plus one confirm | Hugo: "after the first call I should have a button to fetch the ballpark ... the system hears the call, sees what the agent said ... and then we have the solid ballpark for a callback." The four-audit sweep found the whole chain was manual: /api/reprice existed and NOTHING called it, no writer for next_step 'Offer call' existed anywhere, and the figure on the call-two strip was still the overnight screening number. Now: "Fetch ballpark" on every discovery-stage board card, POST /api/crm/fetch-ballpark HEARS THE CALL (latest wk_live_transcripts + typed checklist), extracts condition/works/stated-size with the model (language only, unknown stays unknown), POSTs the facts to the engine's /api/reprice (the only place money is computed), and shows Hugo the preview: the agent's own quotes beside the band or the refusal. On confirm it arms call two: money keys in the same format the priced assign writes, next_step 'Offer call' (this route is deliberately the ONLY writer of that step), board card to Ready for call 2, brief rewritten. An engine refusal (condition unknown, needs a builder, comps below standard) is shown as the homework's honest answer, not an error. | 2026-08-15 |
| 65 | The after-call ballpark holds the SAME evidence standard as the nightly gate | Audit finding: shortlist_gate refuses good/fair/last_resort comps tiers, but reprice.py priced on ANY tier course_comps returned, so the number most likely to be said out loud to a human held a lower standard than the one that only decides who gets rung. reprice.py now refuses tiers below gold/strong, naming the tier it found; pinned by test_reprice.py (37 engine tests green). Discovery branches whose comps sit at 12-24 months get a refusal until fresher sales land, which is the course's own rule, not a bug. | 2026-08-15 |
| 66 | Two offer-mode leaks closed, and the call-two opener now books the builder | Leak one: outcomes follow_up and callback mapped to 'Chase the agent', an offer-mode step, so pressing "Call back" on a FIRST call flipped the card into offer mode (CALL 2 script pane, offer email armed). They now leave the step alone: a callback says nothing about progress. Leak two: ANY property email stamped offer_sent_at/offer_price via MidCallSmsSender, so a call-one files chase recorded an offer nobody made; and the "Write the offer with AI" button rendered on discovery calls. Both now require callModeForStep === 'offer'. Script: the ballpark line at the top of call two now says the next step our end is booking our builder in, per Hugo 2026-08-15: the ballpark question is the doorstep of a real viewing, and it only reads as reasonable when the agent can hear what it is for. | 2026-08-15 |
| 67 | The investor's return is decided AT the ballpark, and the launch bug is fixed | Hugo, seeing the first "unknown property" error: "when we are fetching for the ballpark, that's where the investor's return has to be calculated, because that's how we decide if we're gonna move forward or not." Bug first: the board hands the brrr row's own id but the route queried a column brrr_properties does not have; it now looks up by id and speaks to the engine in source_property_id. Then the point: /api/reprice now runs `deal_engine.brrr_btl` (the port of the course's BRR sheet: SDLT, legals, valuation fees, OUR sourcing fee, refinance at 75%) at the opener AND at the ceiling, and returns `rent_needed_pcm`, the monthly rent that clears the Dealers Club 20% ROI at our opening price. The call extractor also catches a stated rent ("it'd let for 650"), which prices the ROI for real; no rent evidence fails the gate honestly instead of passing on an invented number. The modal shows it as one green or amber box: all-in cost, money left in, ROI or the rent target to go and verify. Ops find on the way: the scraper's systemd unit had crash-looped 11,333 times against an ORPHAN python holding port 5050, so service restarts had been deploying nothing; orphan killed by pid, unit owns the port again. | 2026-08-15 |
| 68 | Ripponden Street: the evidence was there all along, two wrong lenses hid it | Hugo: "dive deep, you will find a way, so much info online." The dig found the refusals were OUR blinkers, not missing data. Lens one: the comps table held five same-street sales inside 240m/12mo (Jowett Street, Alva Road, Radcliffe Road, Ripponden Road) filed under `sale_target`, the RETIRED add-a-bedroom strategy's bucket, whose bedroom counts are often inferred from EPC floor area; reprice only read the same-beds bucket and went begging at 800m. Now all sold comps go to course_comps, whose own rules (style, size within 25%, distance, recency) ARE the course standard, with a hard guard: a bucketed comp counts only when BOTH it and the subject carry a real floor area, so a bigger house can only enter the per-sqm maths, never a size-blind median (the Orion Way shape stays dead). Lens two: the call covered kitchen/bathroom/windows but never the big four, so condition was honestly unknown; the photo eye had read the SAME house the day before (high confidence, cosmetic, flooring) and that read is the evidence every nightly priced deal already ships on, so reprice now falls back to it, call works ride on top, a call band always beats photos, a doubtful read still refuses, and `condition_source` says which one priced it. Result, live: GDV £130,147 on 5 strong per-sqm comps, open £96,197, ceiling £102,610 against £140,000 asking, investor leaves ~£11k in, needs ~£690 pcm rent for 20% ROI. Ops: the orphan-on-5050 disease wedged the unit TWICE in one day (11,333 failed restarts historically); a clear-port drop-in (`fuser -k` before every start) makes it structurally impossible. 107 engine tests green. | 2026-08-15 |
| 69 | The checklist is house-aware: the card only asks what the machine could not answer | Measured first: 232 connected property calls in 7 days, median 92s, but yesterday's average doubled to ~4 minutes after the four-questions-plus-email script shipped, and the Ripponden call ran 10m01s, mostly collecting facts the machine already had (size on the advert, condition on the photos). Hugo: "are we asking information that does not matter?" The SCRIPT stays fixed (same words, same drills, same grading); the QUESTION LIST goes dynamic per house: a question with a `knownWhen` answer is hidden and the fact moves to a green "Already known, do not ask" strip Pedro states instead of asks. Only HIGH-confidence machine answers hide a question (the photo condition band is shown but NEVER hides the big four, a photo cannot see a boiler); anything doubtful stays for a human. Four new fields with a home at last (audit gap: the script asked, the answers had nowhere to live): `water`, `floor_area` (hidden when the machine resolved it, RPC now projects `floor_area_sqm`, migration 20260815000001 applied live), `rent_estimate` ("what would it let for", ten seconds, completes the investor ROI at the ballpark), `rejected_offer` (the course's best question: the rejected level is the vendor's floor). Script gains the rent ask and "always get the level" on rejected offers, plus the rule in as many words: if it is not on the card, we know it, do not ask it. | 2026-08-15 |
| 70 | Rent evidence, a reader that cannot fail, the snapshot wipe, and a fresh queue | Four in one evening sweep. RENT: "why didn't it fetch rent" because reprice never read its own rent comps (30,745 on file); now 2+ lets at the subject's own bedroom count feed the ROI as a median (rent_source nearby_lets), lets of other sizes are shown beside the rent-needed target but NEVER fed into the ROI (a 3-bed rent on a 2-bed return is an invented number wearing evidence's clothes), and a rent stated on the call beats everything. READER: "make this solid, never fails": three attempts with backoff, and if all three come back blank or unparseable the extraction degrades to an empty survey instead of erroring, because the engine's photo-condition fallback prices most houses anyway; flag reader_unavailable rides along. SNAPSHOT WIPE: prune-audit-killed.mjs now strips the address keys (not just money) off an emptied branch, because an address on a card reads as a house behind the card; discovery cards excluded, their address points at a live listing on the scraper, not a brrr row. QUEUE: Hugo's fresh-only order executed; measured FIRST: the "134 branches with nothing behind them" were the 150 discovery cards working as designed (their house lives in rm_listings, not brrr_properties, and carries no money BY DESIGN), zero fossil-address cards were queued; rebuilt anyway for freshness: pool rebuilt from live listings minutes before (1,512 branches, 1,083 priceable-now), pending backed up (wk_queue_backup_20260815b) and wiped, 20 priced on top + 150 fresh discovery under, verified 0 money keys on discovery and 0 cards without a live listing URL. | 2026-08-15 |
| 71 | The ballpark is the whole second-call pack: band, rent, ROI, every comparable | Hugo: "tell how much we can rent for approx ... if not found the machine goes and finds it on Zoopla or Rightmove ... it should bring all the comparables, sold and rental, so all you have to do is the second call." The rent ladder in reprice, evidence-first: (1) a rent the agent stated on the call, (2) the median of nearby lets at the subject's own bedroom count, (3) `rm_rent_market`, the outcode-by-bedrooms model rent_model.py fills from Rightmove's own to-rent results with no browser, FETCHED LIVE ON A MISS (about 0.3s) and refused under 5 samples, never a guess, never a neighbouring area. The modal now carries the full pack: sold evidence listed line by line (address, price, sqm, metres, month), nearby lets, the area going rate with its sample and range, ROI against the 20% gate, and the sellable verdict. Ripponden proves the whole chain on one click, live on prod: open £92,125, ceiling £98,267, rent £912 (OL1 2-bed going rate, 8 live listings), ROI 38-40% at our opener, BOTH Dealers Club gates pass, sellable TRUE. If the agent accepts the ballpark, the only remaining step is the builder viewing quoted within our stated budget. | 2026-08-15 |
| 72 | The full project cost: every fee, three scenarios, and the valuation the deal must reach | Hugo specified a daily opportunity system whose core test is "the expected refinance can recover most or all of the total project cost". The existing `deal_engine.brrr_btl` is the course's own BRR sheet, calibrated against real Dealers Club deals, and counts SIX costs assuming a cash buyer. New `project_cost.py` sits beside it (never replacing it) and counts FIFTEEN: bridging (arrangement, monthly interest, exit) or cash, broker, survey, refinance arrangement and legals, and the insurance, council tax and utilities on an empty house through the works, plus voids and maintenance against the rent. Three scenarios, and the CONSERVATIVE column decides, because a deal that only works optimistically is not a deal. Every ballpark now carries the ARV targets: what the house must be worth done up for 100 / 90 / 75 / 50 percent of the cash to come back. TWO REAL FINDINGS. (a) The old numbers were optimistic: Ripponden showed £11k left in, the full stack says £17k base and £27k conservative, and full recycling needs it worth £153,496 against £130,147 today, so it is an 85% recycle, not a 100% one. (b) A modelling bug in Hugo's own formula: ARV = cost / LTV is slightly too kind because the refinance arrangement fee is a percentage OF THE NEW LOAN, so a higher valuation costs more to borrow against and the target chases itself; solved algebraically, round-trip tested, and with the fee set to zero it collapses to the simple formula exactly. Also new: a lender rent-cover stress test at 125 percent, which nothing in the engine checked, so a deal 25 percent below market can no longer look buyable when no lender would fund the refinance it depends on. 128 engine tests green. | 2026-08-15 |
| 73 | The 15 percent lane, and the nightly 150 ranked on the agent's own words | Hugo, 2026-08-15: "we negotiate 15% off always, then it's not so much of a lowball", and "we need the 150 so Pedro can call tomorrow". TWO CHANGES, both on the engine. (1) `recycle_lane.py`: the course gate prices off a discount to TRUE value (open at TMV x 0.75), which is a 25 to 32 percent ask and passes 31 of 40,252. This lane asks instead whether offering 15 percent under ASKING still returns the investor's capital on refinance; measured, 15 percent off moves the done-up ratio from ~1.40x to ~1.65x, which is where money recycles, taking qualifying deals from 2 to 21. Opener = 0.85 x asking FLOORED (deal_auditor.OPENER_CAP is 0.85 and kills above it, so a rounded-up opener trips its own auditor; also note the auditor already treated 15 percent under asking as the maximum sensible opener, which independently validates Hugo's rule). Ceiling = LOWER of the course walk-away and the price where recycling drops under 90 percent. Ships only if the OPENER already clears the floor, so no deal depends on winning the negotiation. Conversions excluded (the retired strategy). The lane calls `deal_auditor` ITSELF because these properties failed the priced gate before the auditor ever ran and would otherwise reach Pedro un-judged: it killed 3 of 8 on `comps_disagree`, including Doulton Street, whose advert reads "well-presented" and whose same-street sale was £85,000 against a £196,565 valuation. 5 shipped. (2) `discovery_pool.py` now ranks the nightly 150 on motivated-seller wording first (sold as seen / cash buyers / probate / must be sold / in need of, weighted), then priceable-now, price cut, days listed. Measured first: 2,858 adverts carry that language and only 5 had EVER reached Pedro, because the priced gate demands a valuation and a wreck is the hardest thing to value; on a discovery call no figure is said, so that requirement was backwards. Pool 1,512 branches, 1,085 with motivated wording, 406 strongly. Queue rebuilt (backup `wk_queue_backup_20260815c`): 172 pending = 22 priced on top + 150 discovery, 0 money keys on discovery, verified by SQL. 130 engine tests green. | 2026-08-15 |
| 74 | The discount has to be visible BEFORE we ring, and it is measured like for like | Hugo, 2026-08-15: "all the comparisons are 100k and this property is 70k, so we have all the indicators already ... then after the call we push a little bit more." THE SCREEN CHANGED. It was: work out what a house is worth DONE UP, subtract a guessed refurb, apply a discount. That chain rests on the refurb estimate and the condition read, and both were proven unreliable the same day: the photo eye judged Granton Avenue on **3 photos out of 17** and called a gut job "modernisation" with HIGH confidence, pricing the works at £7,839 when the rate card's own full-refurb band says £18,821 and the realistic figure is £28,000. Worse, **606 properties in `rm_property_brain` had their condition decided with ZERO photos**, floor plan only. So the new screen uses neither: asking price versus what the SAME KIND of house actually sold for within 400m, both hard facts (advert + Land Registry). LIKE FOR LIKE OR IT DOES NOT COUNT: a comp must match the subject on style (end-of-terrace prices with terraces; a bungalow is not a detached house) and on bedroom count, because comparing a 2-bed terrace against 3-bed semis is exactly how a house looks 45% underpriced when it is merely smaller, which is the Orion Way and Doulton Street bug. Bounded at both ends: under 15% there is nothing to source, over 45% the cause is almost never a motivated vendor but a comp set that does not belong to the house. Ranked by discount, then motivated-seller wording, then price cuts. Pool 1,512 to 350 branches, every one 15% to 44% under its own like-for-like sold median. Queue rebuilt (backup `wk_queue_backup_20260815d`): 172 = 22 priced + 150 discount-screened, verified 0 money keys on discovery and 0 missing listing links. The offer rule Hugo set alongside it: all money out is the target, £10,000 left in is the hard ceiling, and below 65% of asking we do not call at all. | 2026-08-15 |
| 75 | One discount rule for both lanes, after the priced lane was caught shipping OVERPRICED houses | Hugo asked the right question: "from the 172 branches, what is the minimum discount against the comparables?" Measured on the 22 priced branches then in the queue: **only 7 were under their own like-for-like sold median, and SIX were asking ABOVE it.** Worst was Pendennis Street, Liverpool, asking £135,000 where the same style and bedroom count sold nearby at £103,000, a **31% premium**, sitting in front of Pedro as a deal. Cause: the priced path compares asking to the DONE-UP value, so an overpriced house still reads as a deal once a generous refurb uplift is assumed, and the refurb is the softest number we hold (3 photos of 17 on Granton Avenue; £7,839 for a gut job). Fix: `send_to_elsie.local_discount()` applies the SAME screen `discovery_pool` uses (same style, same beds, within 400m, sold inside 24 months, 3+ comps, asking at least 15% under their median) before anything ships. No like-for-like evidence is an ABSENCE not a refusal, because the tier gate has already had its say; withdrawals are never blocked. Verified: Pendennis BLOCKED at -31%, Granton Avenue sends at 26% under, Fairspring at 23%. Queue cleaned by hand the same night: 15 failing priced branches removed and backfilled with discount-screened discovery, so **every one of Pedro's 172 is now at least ~15% under its like-for-like local sold median** (165 discount-screened + 7 priced that pass the same floor). | 2026-08-15 |
| 76 | The ballpark looks at the house properly: every photo, plus the call | Hugo, 2026-08-15: "when we fetch the ballpark we have to have a proper analysis with the photos and everything ... the machine has to go to the server and really dig into everything." He was right that it was fragile: the fetch re-ran the ARITHMETIC but reused the overnight photo read, which looks at THREE photographs (and at ZERO on 606 properties in `rm_property_brain`, floor plan only). That budget is correct for judging 40,000 houses and wrong for pricing the one house Pedro just spent four minutes on, because condition sets the refurb and the refurb sets the offer. NEW `deep_read.py`: on a fetch the eye sees EVERY photograph (25 cap) and is shown what the agent said on the phone, told to trust the agent over its own reading where they contradict; `property_brain.read_property/survey_property/build_parts` gained `extra_context` for that. The fresh read is compared with the overnight one and a gap of 2+ bands is surfaced as `disagrees` rather than silently resolved, because two readings that far apart mean nobody understands the house yet. PROVED on Granton Avenue (17 photos): the 3-photo read saw kitchen/bathroom/flooring/garden at £7,839; the full read also sees **rewire and replaster** and prices £12,224, 56% higher. ALSO CAPPED, found while testing: `reprice` never opens above 85% of asking and never sets a walk-away above asking. The course arithmetic had produced an opener of £131,519 against a £145,000 ask (91%, no discount worth having, and a figure `deal_auditor.OPENER_CAP` would itself refuse); now £123,250, exactly Hugo's 15% rule. One honest correction to an earlier claim: the photo COUNT was not why the band read "modernisation" (the full read agrees it is modernisation); the under-pricing came from the rate card's modernisation package having no plaster and no decorating line, which remains open. | 2026-08-15 |
| 77 | The offer IS the all-money-out price; 15% under asking is only the cap | Hugo, correcting me 2026-08-15: "we are gonna try to go for all money out first ... the hard thing is not 10k left in the deal, the hard thing is TEN PERCENT, that is what is inside the course." So the target is not a discount off true value, and 15% under asking is a CEILING on how high we ever open, never the aim. On a house needing heavy work the all-out price sits well below it, and that is the number Pedro says. `reprice` now takes the LOWEST of (all money out, TMV x 0.75) as the opener, and the LOWEST of (10% left in, TMV x 0.80) as the walk-away, reporting every input in `offer_basis` so nothing is hidden. Granton Avenue proves it: works of £12,224 push the opener to £112,425, **22% under asking, not 15%**, exactly as Hugo predicted. Also capped the same day: never open above 85% of asking, never walk above asking itself (the course arithmetic had produced £131,519 against a £145,000 ask, 91%, which `deal_auditor.OPENER_CAP` would itself refuse). Two tests rewritten to pin the new rule rather than the old TMV arithmetic. | 2026-08-15 |
| 78 | The price history has a reader at last, and it honestly returns nothing yet | Hugo: "your machine writes 43,000 rows of listing changes nightly and nothing reads them, so reduced three times isn't visible. You have to do that as well." Correct, and now `price_history.py` reads it: observed cuts, total drop from the highest price seen, biggest single cut, and fall-through detection (went sstc or under offer, came back). It uses the SCRAPER'S underscored status vocabulary (`under_offer`, not "under offer"), which is precisely the mismatch that made two earlier attempts at this silently never fire. **It returns zeros today and says so out loud.** Measured: `rm_listing_history` spans 2026-08-12 to 2026-08-15, 43,813 rows over 40,252 properties, of which 36,691 have ONE snapshot and 3,561 have two, and **zero have had their price move in that window**. Faking a count off the Rightmove `reduced` flag would say "1" for all 12,357 flagged properties and add nothing to what `discovery_pool` already ranks on. The value accrues nightly; in a few weeks "reduced three times in four months" becomes a real sort key. Wiring it now is what makes that possible later. | 2026-08-15 |
| 79 | The rate card gets the three lines every refurb has, at the cheapest honest rate | Hugo, 2026-08-15: "fix it then, the offer is reading too high ... but don't go for high end, those properties are shipping properties which is gonna be cheap labour ... everything is on the cheapest side of every labour." The card priced a kitchen, a bathroom, floors, doors, a garden tidy and gutters, and stopped. **It had no painting line at all, in any band**, and "modernisation" carried no plastering. A house cannot be refurbished without being painted, so every estimate was short, every TMV too high, and every offer too generous by roughly a pound for a pound. Added, all rental spec at Hugo's own crew rate: `decorate` (£1,600 trade, whole house, trade white, woodwork glossed), `skim_patch` (£1,750, strip woodchip, skim over artex, patch the bare), `waste_skips` (£600, two or three skips and the labour to fill them). Cosmetic and modernisation gain decorate and skips; modernisation also gains skim_patch; full_refurb takes the whole card EXCEPT skim_patch, which beside its existing `replaster` would be paying twice for one wall. Band trade totals: cosmetic £2,700 to £4,900, modernisation £9,200 to £13,150, full_refurb £24,000 to £26,200. **Structural point worth keeping:** the course priced twelve lines on camera and a test pins every one; these three the course never mentioned, so they live in a separate `OUR_TRADE_PRICES` with their own pinning test, and a new test refuses any line belonging to neither list. Inventing a "course price" for a line the builder never gave would have destroyed the only external check the rate card has. Granton Avenue end to end: works £7,839 to £15,194, opener £123,250 to **£109,455, which is 25% under asking rather than 15%**. 249 engine tests green. | 2026-08-15 |
| 80 | The standing brief: our email left with every branch, on both calls | Hugo, 2026-08-15: "always tell the agent I'm sharing the email, for the agents to keep an eye when the properties that need discounts and plenty of refurb to contact us directly." So it is now a step, not an instinct, and it is on BOTH calls. Call one says it immediately after the email Pedro sends while still on the phone has landed, so our address is on their screen while he asks. Call two says it last, before the goodbye, and it matters most on the calls that DIED on price: if the house is gone the relationship is the only thing left to take, and a branch that has spoken to us twice is the one that rings about the next one. The brief is deliberately only two things, needs plenty of work and the price has to come down, because those are the two a negotiator can actually spot; a list of criteria is what gets you filed as an investor. **It is explicitly NOT their mailing list**, which is every house that reaches Rightmove and which the script already teaches as a brush-off, so both the mailing-list panel and the new step say so out loud, or he accepts the brush-off and believes the step is done. All three surfaces agree, as the standing rule requires: the script HTML (stage 5 and the close of call two), the live coach (a standing-brief rule plus the Discovery and Offer step prompts, fired hardest when a call is winding down without it), and the 5:30 report, which counts `left_the_standing_brief` and needs BOTH halves inside a two-turn window so "send me the floor plan" plus the condition question cannot score it. No migration: `wk_property_call_script.html` is still NULL in production, so the repo file is what Pedro reads and the edit is the deploy. Verified live at heyelsie.com/scriptcall1, coach function redeployed, 2,870 tests green. Separately, and worth recording because it was checked: the Fontaine course sets **no daily call quota anywhere** (10 agents a week as practice, 50 only as a mindset example, and the real instruction is two or three agents rung every Monday), and it never rings landlords at all, so this relationship step is the closest thing it has to a volume strategy. | 2026-08-15 |
| 81 | The biggest refusal in the pipeline was a workaround away from being solved | `no_agent_phone` binned **18,573** listings before anybody worked out whether they were worth buying, more than auction, tenanted and no-floor-plan **combined**, and it had been treated as a fact of life. It was never a parsing bug: 16,681 had no phone AND no agency name AND no description, because **the detail page had never been fetched**. That needed Playwright, which needed a residential proxy, and the proxy had been dead since 11 August. The response at the time was an elaborate Zoopla bridge to recover agency names from another portal; on its last run it rescued **three** phones, because its own input needs headed Chrome through the same dead proxy. MEASURED 2026-08-15: Rightmove answers a detail page to a plain `urllib` GET from the VPS in about 0.3s, no browser and no proxy. A random sample of 40 binned listings returned 39 live, and **all 39 gave up a phone, a name, a description and photographs**; the fortieth was a genuine 410. Full backfill run at ~6 pages/sec. Two traps, both now written into `page_model.py`: the blob was renamed to `window.__PAGE_MODEL` with **two** underscores, so `_extract_listing_details`'s `/window\.PAGE_MODEL\s*=/` matched nothing (the file's own line 33 already knew this and the second reader was never updated); and the payload is **flattened**, every object value being an index into an array, so a regex plus `json.loads` yields a tree of integers and no phone number. New `page_model.py` (pure, 14 tests, no network), `backfill_agent_direct.py` (resumable, stops on repeated 403s, marks 404/410 as withdrawn, writes facts only and leaves `is_auction`/`is_tenanted` to `listing_sanity`'s tuned detectors). Zoopla rescue commented out of the nightly with the reasoning. **The lesson worth keeping: a refusal that large deserved somebody asking why, not a workaround.** | 2026-08-15 |
| 82 | The offer is priced on the trade rate; our crew's rate is the builder's budget | Hugo: "the offer is reading too high ... price it off the higher end, but I want the builder to do the work on budget as well, I don't want deals stuck on the shelves." `reprice.py:328` took `cost["low"]`, materials plus labour at Hugo's own crew rate (LABOUR_FACTOR 0.65). Because TMV = GDV minus refurb, **a low refurb produces a HIGH offer**, pound for pound, and the file's own docstring records that sensitivity. So every offer assumed the cheapest possible outcome and then went out as though it were certain. The course says the opposite in as many words: "you'd rather be a bit more over than under", and "I would use them prices and go slightly above those." Changed in ONE place per path so the nightly and the ballpark can never disagree: `deal_engine.appraise` prices on `refurb_trade` when it has one (falling back to `low` and FLAGGING that it did, because a missing number must not become a free refurb), and `reprice` takes `cost["trade"]` while carrying `refurb_builder_budget` alongside. `at_trade_rate` becomes `at_crew_rate`, the upside rather than the downside. HONEST CONSEQUENCE, recorded rather than tuned away: `appraise` judges strategies at the ASKING price, and at asking with a trade refurb the pinned test house returns 18.93% ROI against a 20% gate, while at the price we would actually open at it returns 22.86%. `sellable` gates nothing in the pipeline (the gate refuses on `engine_*`, `ceiling_price_rule` and the auditor), so the pool does not shrink; a test now pins that, and warns anybody who wires the flag into the gate that they would be screening on a price we never pay. | 2026-08-15 |
| 83 | The list stopped putting the houses Pedro cannot close at the top | Found by cross-checking our screen against the course. `discovery_pool` screened and ranked on the raw discount to the local sold median and read **no condition and no refurb at all**. Of two houses both asking GBP 85,000 into a GBP 100,000 median, one needing GBP 15,000 of work needs a 13% talk-down and one needing GBP 30,000 needs 28%, and the ranking put the 28% one FIRST. That is exactly the lowball the course spends a module warning against, so the top of the list was systematically the part that could not close. NOW: the condition band comes from the ADVERT'S OWN WORDS (free, no photos, no floor plan, and the agent writing "in need of full renovation" is stating a fact against their own interest), it is priced through the rate card at the trade rate, and the list is ordered by the **required talk-down, ascending**. Defaulting to `modernisation` when the advert says nothing is deliberate: assuming turnkey would price the work at zero and float an unknown house to the top. **The hard 15% discount gate is untouched**, so a guessed refurb can reorder the list or drop an impossible house and can never admit one. MEASURED BEFORE THE THRESHOLD WAS SET (492 branches): talk-down runs from **-32.4% to 48.5%, median 10.2%**, and a negative figure means we could pay MORE than they ask and it still works. Survivors: 348 at a 15% cap, 460 at 20%, **484 at 25%**. So the cut drops eight branches and the comment says plainly that the value is in the ORDERING; it is deliberately not tightened to 15%, which would throw away 29% of a pool that is already the binding constraint on 150 calls a night. One rank key now serves both the one-house-per-branch pick and the across-branch sort, which were two separate expressions that could drift. | 2026-08-15 |
| 84 | The two facts the call always gathered and always threw away, and going national | THE AGENT'S COMPARABLE. The script has asked "has anything on that street sold recently that was done up, what did it go for" since section 8b, calls it "the most valuable question on this page", the coach prompts for it and `next-step-brief` names its absence as the thing holding a deal back. **There was no field.** It went in free text and nothing read it. Now `agent_comparable` on the checklist, and it is a ONE-WAY cross-check on the end value: lower than ours we take theirs (they know the street and it is against their interest to understate it), higher we record it and change nothing, because the witness is the person selling us the house and believing one sentence upward is exactly how Orion Way reached GBP 293,000. THE REJECTED OFFER was captured on 15 August but dead downstream: `fetch-ballpark` forwarded four keys, one of which (`'notes'`) matched nothing at all because the free text lives on a column and not in the JSON. It is the vendor's floor, so it can kill a deal ("DEAD ON ARRIVAL, they refused X and our maximum is Y") and never lift our number. **Both travel as engine ARGUMENTS, never inside `call_notes`**, which goes only to the vision model and is money-redacted first, so "number 12 went for 118" was arriving as "number 12 went for [price removed]". The ballpark now forwards every question on `QUALIFICATION_QUESTIONS`. THE BALLPARK also grew the hard excludes the nightly always had (it would price a retirement flat) and now accepts the same 24-month evidence at 400m that the list producing the call accepts, which needed a `tiers` argument on `course_comps.select` because it widens the RADIUS to 800m before it widens the age, so "a quarter mile whatever the age" was not expressible by filtering the answer. NATIONAL: 305 towns resolved from a reviewable NAME list, each verified against the county its answer must mention, because a bare "St Helens" resolves to the village on the Isle of Wight and "Gillingham" to the Dorset one. They cover **187,914 live listings against the 40,252 we hold**. Written to `rm_config.national.json` and deliberately NOT into the nightly's config: 305 searches on the Playwright path is about four hours inside a window ending 06:10. The browserless search reader is the intended path and **carries the agent's phone on 100% of rows**, so the biggest refusal cannot recur on new stock. | 2026-08-15 |
